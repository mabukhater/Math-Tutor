# Copy to web/.env.local (gitignored). Browser-safe values are NEXT_PUBLIC_*.
NEXT_PUBLIC_SUPABASE_URL=https://<ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-side only — used by API routes to read questions + write progress. NEVER NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
